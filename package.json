{
  "private": true,
  "name": "lib-monorepo",
  "version": "0.0.0",
  "main": "index.js",
  "workspaces": [
    "playground/*",
    "packages/*"
  ],
  "scripts": {
    "bumper": "npm_config_registry=https://npm.pkg.github.com npx @omariosouto/bumper",
    "example": "npm exec --workspace=example-nextjs",
    "dev:example": "npm run example -- npm run dev",
    "build:example": "npm run example -- npm run build",
    "test:example": "npm run example -- npm run test",
    "lib": "npm exec --workspace=@omariosouto/common-ui-web",
    "build": "npm run lib -- npm run build",
    "test": "npm run lib -- npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
